import "styled-components"

declare module "styled-components"{
    export interface DefaultThema{
        bgColors: string;
        textColor: string;
        accentColor: string;
        cardBgColor: string;
    }
}